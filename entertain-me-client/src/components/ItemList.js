import React from 'react';
import ItemContainer from './ItemContainer'

const ItemList = ({movies}) => {
  // const Item = movies.map(movie => <ItemContainer movie={movie} key={movie._id}/>)
  return (
    <div>
      {/* <Item /> */}
      {/* { Object.keys(movies) } */}
      <h1>asd</h1>
    </div>
  );
}

export default ItemList;
